/** Small orange dot marking a "modificador variado" (as opposed to a fixed calculation component). */
export function VariedDot() {
  return (
    <span className="h-1.5 w-1.5 shrink-0 rounded-full bg-amber-500" title="Modificador variado" />
  );
}
